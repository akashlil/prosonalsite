/** @type {import('next').NextConfig} */
// const nextConfig = {
module.exports = {
  reactStrictMode: true,
  swcMinify: true,
  env: {
    BASE_URL: "https://",
  },
};

// module.exports = nextConfig
